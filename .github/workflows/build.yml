name: Build OneCloud ImmortalWrt (Kernel 6.6)

on:
  schedule:
    - cron: '0 16 * * *'  # 每天北京时间 00:00 自动构建（UTC+8）
  workflow_dispatch:
    inputs:
      kernel_version:
        description: '选择内核版本'
        required: true
        default: '6.6'
        type: choice
        options:
          - 6.6
          - 6.7
          - 6.9

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 设置构建环境
        run: |
          sudo apt update
          sudo apt install -y build-essential ccache curl git unzip wget python3 gzip xz-utils

      - name: 下载源码
        run: |
          git clone https://github.com/immortalwrt/immortalwrt.git -b openwrt-24.10 openwrt
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 应用自定义 diy.sh
        run: |
          chmod +x diy.sh
          cd openwrt
          ../diy.sh

      - name: 生成默认配置
        run: |
          cd openwrt
          make defconfig

      - name: 编译固件
        run: |
          cd openwrt
          make -j$(nproc) || make -j1 V=s

      # ✅ 打包线刷镜像（整合 eMMC 可刷 img）
      - name: 打包线刷镜像
        run: |
          cd openwrt/bin/targets/*/*
          mkdir -p release

          # 找到 ext4 镜像
          IMG=$(ls *-ext4-sysupgrade.img.gz | head -n 1)
          if [ -z "$IMG" ]; then
            echo "❌ 未找到 ext4 镜像，无法生成线刷包"
            exit 1
          fi

          # 解压
          gunzip -c "$IMG" > rootfs.img

          # 模拟 eMMC 分区结构（可按需调整分区大小）
          echo "🧩 生成 eMMC 线刷镜像..."
          dd if=/dev/zero of=onecloud_emmc_flash.img bs=1M count=2048
          parted onecloud_emmc_flash.img --script mklabel gpt
          parted onecloud_emmc_flash.img --script mkpart boot ext4 1MiB 64MiB
          parted onecloud_emmc_flash.img --script mkpart root ext4 64MiB 100%

          # 写入 rootfs 到对应偏移位置（假设64MiB偏移）
          dd if=rootfs.img of=onecloud_emmc_flash.img bs=1M seek=64 conv=fsync

          # 压缩为最终线刷包
          gzip -c onecloud_emmc_flash.img > release/onecloud_emmc_flash_${{ github.event.inputs.kernel_version || '6.6' }}.img.gz
          echo "✅ 线刷包生成完成"

      # ✅ 自动发布到 GitHub Releases
      - name: 发布到 GitHub Releases
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: "build-${{ github.run_id }}"
          name: "OneCloud ImmortalWrt 线刷包 (Kernel ${{ github.event.inputs.kernel_version || '6.6' }})"
          body: |
            自动构建的 OneCloud ImmortalWrt 线刷包  
            - 内核版本：${{ github.event.inputs.kernel_version || '6.6' }}  
            - 编译时间：${{ github.run_started_at }}  
            - 设备：OneCloud  
            - 分支：openwrt-24.10  
            - 镜像格式：`.img.gz`（可直接 dd 或 USB Burning Tool 刷入 eMMC）
          files: |
            openwrt/bin/targets/*/*/release/*.img.gz

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
