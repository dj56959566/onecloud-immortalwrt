name: Build OneCloud ImmortalWrt (Extreme Space Optimized)

on:
  schedule:
    - cron: '0 16 * * *'   # 每日北京时间 00:00 自动构建
  workflow_dispatch:
    inputs:
      kernel_version:
        description: '选择内核版本（默认 6.6 LTS）'
        required: true
        default: '6.6'
        type: choice
        options:
          - 5.15
          - 6.6
          - 6.10
          - 6.12
          - 6.13

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
      START_TIME: ${{ github.run_started_at }}

    steps:
      - name: 🧹 Full Advanced Cleanup (Free up >20GB)
        run: |
          echo "🚧 Cleaning runner environment to maximize space..."
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc || true
          sudo rm -rf /usr/share/doc /usr/share/man /usr/share/locale || true
          sudo docker system prune -af || true
          sudo apt-get autoremove -y || true
          sudo apt-get clean || true
          rm -rf ~/.cache/*
          sudo rm -rf /var/lib/apt/lists/* || true
          sudo rm -rf /tmp/* || true
          echo "✅ After cleanup:"
          df -h /

      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Set kernel version
        run: |
          echo "KERNEL_VERSION=${{ github.event.inputs.kernel_version }}" >> $GITHUB_ENV
          echo "Kernel version set to $KERNEL_VERSION"

      - name: 🧰 Install essential dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential gcc g++ git wget curl unzip \
            python3 zstd rsync libncurses5-dev gawk gettext \
            libssl-dev xsltproc file bzip2 ccache
          df -h /

      - name: 🗄️ Setup tmpfs build directory (8G RAM)
        run: |
          echo "🧩 Mounting tmpfs (8G) for build workspace..."
          sudo mkdir -p /mnt/workspace
          sudo mountpoint -q /mnt/workspace || sudo mount -t tmpfs -o size=8G tmpfs /mnt/workspace
          mkdir -p /mnt/workspace/openwrt /mnt/workspace/downloads /mnt/workspace/staging_dir
          echo "WORKDIR=/mnt/workspace/openwrt" >> $GITHUB_ENV
          echo "DL_DIR=/mnt/workspace/downloads" >> $GITHUB_ENV
          echo "STAGING_DIR=/mnt/workspace/staging_dir" >> $GITHUB_ENV
          df -h /mnt/workspace

      - name: 📥 Clone ImmortalWrt source
        run: |
          cd /mnt/workspace
          git clone https://github.com/immortalwrt/immortalwrt.git openwrt
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 🧩 Apply config and custom diy.sh
        run: |
          cd /mnt/workspace/openwrt
          cp $GITHUB_WORKSPACE/config/seed.config .config
          chmod +x $GITHUB_WORKSPACE/scripts/diy.sh
          $GITHUB_WORKSPACE/scripts/diy.sh || true

      - name: 🧪 Check root disk space
        run: |
          avail=$(df / | tail -1 | awk '{print $4}' | sed 's/G//')
          echo "Available root space: ${avail}G"
          if [ "${avail%.*}" -lt 5 ]; then
            echo "❌ Root disk space <5G, exiting build."
            exit 1
          fi

      - name: 🚀 Build firmware
        run: |
          cd /mnt/workspace/openwrt
          echo "Using KERNEL_VERSION=$KERNEL_VERSION"
          make defconfig

          # 限制并行线程防止空间爆掉
          NPROC=$(nproc)
          if [ $NPROC -gt 2 ]; then NPROC=2; fi

          # 检查剩余空间决定编译模式
          ROOT_FREE=$(df / | tail -1 | awk '{print $4}' | sed 's/G//')
          if [ "${ROOT_FREE%.*}" -lt 6 ]; then
            echo "⚡ Root space <6G, only building sysupgrade"
            make -j$NPROC DL_DIR=$DL_DIR STAGING_DIR=$STAGING_DIR V=s sysupgrade || make -j1 DL_DIR=$DL_DIR STAGING_DIR=$STAGING_DIR V=s sysupgrade
            echo "✅ Sysupgrade-only build finished"
          else
            echo "✅ Enough space, building full firmware"
            make -j$NPROC DL_DIR=$DL_DIR STAGING_DIR=$STAGING_DIR || make -j1 DL_DIR=$DL_DIR STAGING_DIR=$STAGING_DIR V=s
          fi

      - name: 📦 Package firmware and cleanup tmpfs
        run: |
          mkdir -p release
          cp -r /mnt/workspace/openwrt/bin/targets/*/*/* release/ || true
          cd release

          END_TIME=$(date '+%Y-%m-%d %H:%M:%S')
          BUILD_TIME=$(( ($(date -d "$END_TIME" +%s) - $(date -d "${{ env.START_TIME }}" +%s)) / 60 ))

          echo "OneCloud ImmortalWrt 自动构建完成" > release_notes.txt
          echo "构建时间: $END_TIME" >> release_notes.txt
          echo "构建耗时: ${BUILD_TIME} 分钟" >> release_notes.txt
          echo "内核版本: $KERNEL_VERSION" >> release_notes.txt
          echo "" >> release_notes.txt
          echo "文件列表：" >> release_notes.txt
          ls -lah >> release_notes.txt || true

          sha256sum * > checksum.sha256 || true

          SYSUPGRADE=$(ls *sysupgrade.bin 2>/dev/null | head -n1 || true)
          SDCARD=$(ls *sdcard.img.gz 2>/dev/null | head -n1 || true)

          # 根据构建模式判断是否打包 SD 卡镜像
          if [ -n "$SYSUPGRADE" ]; then
            zip -j onecloud-immortal-${KERNEL_VERSION}-sysupgrade.zip "$SYSUPGRADE" checksum.sha256 release_notes.txt || true
          fi
          if [ -n "$SDCARD" ] && [ "${ROOT_FREE%.*}" -ge 6 ]; then
            zip -j onecloud-immortal-${KERNEL_VERSION}-sdcard.zip "$SDCARD" checksum.sha256 release_notes.txt || true
          fi

          # 合并总包
          zip -r onecloud-immortal-${KERNEL_VERSION}.zip ./*.zip || true

          # 🔥 清理 tmpfs 中的 bin 文件，释放空间
          cd /mnt/workspace/openwrt/bin || true
          rm -rf * || true
          echo "✅ tmpfs bin 文件已清理，仅保留 release zip"

          echo "✅ 打包完成："
          ls -lah release

      - name: 🧹 Delete old releases (keep latest 0)
        uses: dev-drprasad/delete-older-releases@v0.3.4
        with:
          keep_latest: 0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🚀 Publish new release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.zip
          tag_name: nightly
          name: "OneCloud ImmortalWrt 自动构建 - Kernel ${{ github.event.inputs.kernel_version }}"
          body_path: release/release_notes.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ♻️ Cleanup & unmount tmpfs
        if: always()
        run: |
          echo "🧹 Unmounting tmpfs build directory..."
          sudo umount /mnt/workspace || true
          sudo rm -rf /mnt/workspace || true
          df -h
          echo "✅ tmpfs released and workspace cleaned."
